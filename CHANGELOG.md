# WEDOAUDIO — история версий

## 4.2.2-rc1 — кандидат, 11.09.2026

- Исправлены Tempoac OFF, возврат Onsetrel и гейт широкополосного onset.
- Defaults sensitivity/refractory согласованы с DSP; диапазоны ползунков теперь соответствуют единицам.
- Исправлено сведение стерео через chanop=avg, проверен сигнал только справа.
- Холодный старт явно запрашивает входную цепочку; отключение очищает выход и сохраняет 22 канала.
- Добавлены проверки некорректных численных входов и lifecycle управляющих параметров.
- Native 2025.32460: 7/7; ядро 14/14, тембр 14/14, новые тесты 5/5, полный музыкальный стенд без регрессии.
- Шина и имена каналов сохранены. Timbre/MFCC/stems не включались в portable-компонент.
- Кандидат не опубликован.


- **4.2.1 · форматы и живой прогон (09.09.2026)** — два новых стенда, и второй нашёл дефект,
  которого не видел ни один из четырёх прежних.
  **Форматы** (`src/_wedoaudio_formats.py`, отчёт `FORMAT_REPORT.md`): 72 ритмических файла с темпом
  в имени и 36 неритмических из размеченной библиотеки лупов Ableton, каждый в девяти форматах —
  wav16/24/32f, flac, mp3 320 и 128, ogg q5, opus 96 и пересчёт в 48 кГц. Истина точная и ничьего
  мнения не требует: одна музыка в разных контейнерах обязана дать один ответ. Без потерь ответ
  не меняется вообще — дрейф темпа 0.0000%%, F-мера 1.00 на всех 288 прогонах. С потерями типичный
  файл тоже не меняется (медиана дрейфа 0.00%% у всех кодеков), но появляется хвост: mp3-320 сбивает
  метрический уровень на 3 файлах из 72, ogg — на 7, mp3-128 — на 8, opus-96 — на 12. Среднее по
  формату доходит до 5.88%% и именно поэтому в отчёте стоит медиана, а не оно. На неритмическом
  материале — 1.6 ложного кика в секунду: дрон и эмбиенс остаются незакрытой дырой, и это записано
  как есть.
  **Живой прогон** (`live_results.json`): тот же файл через живой кук TouchDesigner, на 15 кадрах
  в секунду, со спектром от самого TD, а не от numpy. Обнаружено, что Audio Spectrum CHOP создаётся
  настроенным ДЛЯ ПОКАЗА — логарифмическая ось частот и подъём верхов, — тогда как ядро переводит
  герцы в номер бина линейно. Полоса кика 40–120 Гц читалась 0.007 при пороге 0.05: **кик не
  срабатывал ни разу** (0.00/с против 2.16/с в офлайне), ошибки не было нигде, а офлайновый стенд
  продолжал показывать 18/18, потому что он никогда не берёт спектр у TouchDesigner. После
  приведения оси к линейной и длины к 1024 бинам: 2.35 кика в секунду, темп 150.0 против офлайновых
  151.4. Геометрия спектра закреплена в сборке, записана контрактом в ядре и проверяется
  самопроверкой компонента — подмена оси теперь попадает в список замечаний.
  Побочно исправлено: dt считается по часам, а не по `project.cookRate` (заявленная частота кадров
  не равна фактической), плитка компонента показывает свою шину признаков (`./out_features` —
  голое имя ищет соседа, а не ребёнка).
  Стенд не сдвинулся: кики 18/18, снейры 18/18, темп 12/15.

- **4.2.1 · ревизия переносимости (09.09.2026)** — компонент прошёл ревизию против правил сборки TD, и
  результат разошёлся с ожиданием: жёстко зашитых ссылок наружу оказалось не сто, а одна. Первый прогон
  насчитал 96 «нарушений», потому что у ссылочных параметров `eval()` отдаёт разрешённый абсолютный путь
  даже у относительной ссылки; по хранимому `val` абсолютных ссылок ровно две, наружу ведёт одна. Заодно
  выяснилось, что `findChildren(depth=N)` отбирает РОВНО глубину N, а не «до N» — самопроверка с `depth=4`
  обходила почти весь компонент и всегда была «чисто».
  Найдено и исправлено в переносимой сборке: частота дискретизации была зашита числом 44100 (на 48 кГц все
  полосы уезжают на 8.8%, кик уходит в подвал, ошибки нет) — теперь берётся у входа; время считалось как
  `absTime.frame / 60`, то есть на проекте в 30 кадров темп ошибался ровно вдвое — теперь `absTime.seconds`
  и `project.cookRate`; ошибка анализа глоталась через `except: pass` — теперь пишется в параметр `Status`,
  видимый в интерфейсе; кнопка Reset никого не вызывала — подключена через parexec (`onpulse`, не `pulse`);
  у 30 параметров появились подсказки; внутрь добавлен `agents_md`.
  В ядре: границы полос кика и снейра стали настраиваемыми, но присутствующий нулевой ключ больше не проходит
  как значение — `c.get('kick_lo') or 40.0`. Это защита от ситуации, когда у `Kickbandlo`
  умолчание 0.0: «сброс к умолчаниям» схлопывал полосу кика в 0–0 Гц молча. Стенд после правки не сдвинулся:
  кики 18/18, снейры 18/18, темп 12/15.
  Проверка: тох загружен в пустой контейнер под именем `ANALYZER_X`, 22 именованных канала, самопроверка
  чистая, источник 44.1 кГц и он же через ресемпл 48 кГц — компонент сам переключился и продолжил считать.

- **4.2.1 · четвёртый заход (08.09.2026)** — проверка на размеченной библиотеке сэмплов (`src/_wedoaudio_libtest.py`, аудио в репозиторий
  не кладётся): 208 файлов пользовательской библиотеки Ableton Live, разметка из имён — темп, инструмент, тоника.
  Темп на ритмическом материале 12/32, и рядом всегда печатается линейка: те же файлы через librosa 13/32, двенадцать
  не берёт никто. Тональность 13/22 на полнодиапазонном материале и 3/13 на басовых лупах — второе честный провал
  (основной тон ниже окна хромы; понижение границы до 30 и 25 Гц проверено, становится хуже). Разделение
  инструментов: кик-лупы 1.40 кика/с против 0.05 снейра/с, но дрон даёт 2.27 ложных кика/с — тянущийся материал
  остаётся незакрытой дырой. Гармоническое подкрепление автокорреляции темпа: библиотека 18 → 24 из 65.
  Мел-логарифмическая кривая новизны для темпа откачена: выигрыш на пробнике из 8 файлов не подтвердился на 65,
  а стенд просел с 12/15 до 9/15. Добавлен `requirements-dev.txt`.
  Компонент пересобран из текущего ядра, проверка загрузкой в пустой контейнер с ДРУГИМ именем —
  ноль ошибок.
- **4.2.1 · третий заход (08.09.2026)** — проверка на аудио, которого мы не делали (`src/_wedoaudio_realtest.py`, нужны librosa и ffmpeg):
  точная разметка через `librosa.clicks` 6/6, настоящая музыка против `librosa.beat.beat_track` 4/4 по метрическому
  уровню, устойчивость к контейнеру 6/6 с нулевым дрейфом (wav16/24/32f, flac, mp3, ogg, opus, 44.1 и 48 кГц).
  Найдено и исправлено ещё два дефекта: канал `rawbeat` был мёртв (порог содержал слагаемое 0.02, а поток энергии
  живёт в 0.004–0.03 — ноль срабатываний на клик-треке и на ровной бочке, два за 30 с драм-н-бейса; после перевода
  на относительную шкалу F 0.98 против точной разметки) и темп из медианы интервалов был хрупким (совпадение с
  librosa 1/4, дрейф 5.9% от простого пересжатия в 48 кГц). Темп теперь из автокорреляции кривой новизны с
  логнормальным приором вокруг 120 BPM, диапазон объявлен 68–200; медиана интервалов — запасной путь.
  Стенд по темпу 9/15 → 12/15, вальс стал точным. Все батареи: 14/14, 14/14, стенд 18/18, реальное аудио 6/6.
- **4.2.1 (релиз 08.09.2026)** — та же шина 4.2, свежая начинка: MFCC, тональность и K-взвешенная громкость;
  четыре стенда проверки (синтезированные треки, аудио с точной разметкой от чужого кода, настоящая музыка
  против librosa, размеченная библиотека лупов); пять исправленных дефектов ядра, два сознательных отката.
  Совместимость с 4.2.0: `cfg['onset_rel'] = 0` возвращает прежнее поведение целиком. Ниже — по заходам.
- **4.2.1 · второй заход (08.09.2026)** — стенд на синтезированных треках (`src/_wedoaudio_bench.py`): 6 стилей x 3 формата,
  F-мера онсетов с допуском ±50 мс по порогу MIREX; подбор порогов замером (`src/_sweep.py`); CONTRIBUTING.md.
  Стенд нашёл три дефекта, невидимых старым тестам: кик срабатывал 2-3 раза на удар из-за держащейся басовой
  ноты в полосе 40-120 Гц (точность 0.37 → 0.80-1.00); смена 44.1 → 48 кГц переносила фундамент баса через край
  полосы и меняла счёт с 19 на 47; снейр не детектировался ни на одном стиле, потому что гейт сравнивал максимум
  бина с фиксированной долей шкалы, а шумовой снейр до неё физически не доходит (F 0.00 → 1.00). Окно интервалов
  темпа 0.25-1.5 с → 0.20-2.60 с, разбор метрического уровня по автокорреляции новизны вне 70-180 BPM.
  Откат: `cfg['onset_rel'] = 0` возвращает поведение 4.2.0 целиком. Прежние батареи 14/14 и 14/14.
- **4.2 (релиз 03.09.2026)** — самодостаточный tox: внутренний `device_in`, относительные ссылки, help на 30 каналов,
  импорт в чистый контейнер = 0 ошибок (проверено под двумя разными именами контейнера). Тесты ядра 14/14 PASS.
- **4.2-stems (21.08.2026)** — приём отдельных дорожек, отсчёт уровня по доле дорожки, а не по абсолютной
  величине: абсолютные огибающие 0.01…0.15 просто не читались. Фикс тишины: порог измеряется до гейна.
- **4.2-v2 (20.08.2026)** — тактовая сетка `barPhase / downbeat / bpmConf`, полоса `sub`.
- **4.2 (16.06.2026)** — единый анализатор: один FFT (`spectrum_raw` 8192), Script CHOP-ядро на numpy, AGC
  (95-й перцентиль), онсеты по спектральному потоку с адаптивным порогом, темп + фаза бита (PLL),
  выход шиной CHOP и 128-бинной текстурой. 26 каналов.
- **до 4.2** — полосы считались в нескольких местах и расходились между собой, flux/centroid/flatness
  были константами, детектора кика в общем ядре не существовало вовсе.
